[package]
name = "{{project-name}}"
version = "0.0.1"
edition = "2024"
authors = ["{{authors}}"]
resolver = "3"
rust-version = "1.89"

[profile.release]
debug = true
opt-level = "z"

[profile.dev]
debug = true
opt-level = "z"

[patch.crates-io]
concurrent-queue = { git = "https://github.com/npmenard/concurrent-queue", rev = "ffdd176" }

[dependencies.micro-rdk]
git = "https://github.com/viamrobotics/micro-rdk.git"
version = "0.5.1"
rev = "v0.5.1"
features = [
  "esp32",
  "binstart",
  {% if camera == "true" %}"camera",{% endif %}
]

[dependencies]
log = "0.4"

[build-dependencies]
cargo_metadata = "0.23"
embuild = "0.33"
regex = "1.11"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[package.metadata.esp-idf-sys]
esp_idf_version = "v5.4.2"
esp_idf_tools_install_dir = "custom:target/.embuild/espressif"
esp_idf_sdkconfig_defaults = [
  "sdkconfig.defaults",
]

{% if camera == "true" %}
[[package.metadata.esp-idf-sys.extra_components]]
remote_component = { name = "espressif/esp32-camera", version = "2.1.2"}
bindings_module = "camera"
bindings_header = "bindings.h"
{% endif %}

[[package.metadata.esp-idf-sys.extra_components]]
remote_component = { name = "espressif/mdns", version = "1.8.2" }
